#pragma once
// wifi_config.h — WiFi credentials for ArduinoOTA
// Edit WIFI_SSID and WIFI_PASSWORD to match your network, then flash.
// Do NOT commit real credentials to a public repository.

#define WIFI_SSID     "YourNetworkSSID"
#define WIFI_PASSWORD "YourNetworkPassword"
#define OTA_PASSWORD  ""    // "" = no password required for OTA uploads
